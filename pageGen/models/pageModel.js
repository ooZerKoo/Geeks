const pageModel = {};
const Mongoose = require('mongoose');

pageModel.get = () => {
}

pageModel.put = (data) => {
    
}

pageModel.post = (data) => {
    
}

pageModel.delete = (id) => {
    
}

module.exports = pageModel;